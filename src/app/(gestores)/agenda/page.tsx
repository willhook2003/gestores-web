import { Grid, Typography } from "@mui/material";

export default function AgendaPage() {
  return (
    <Grid container justifyContent={"center"}>
      <Typography variant="h5">Agenda Page</Typography>
    </Grid>
  );
}
